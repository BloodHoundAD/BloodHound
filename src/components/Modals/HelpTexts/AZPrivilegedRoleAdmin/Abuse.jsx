import { typeFormat } from '../Formatter';
const Abuse = (sourceName, sourceType, targetName, targetType) => {
    let text = `Abuse text goes here.`;
    return { __html: text };
};

export default Abuse;