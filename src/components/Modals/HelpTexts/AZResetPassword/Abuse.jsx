import React from 'react';

const Abuse = () => {
    return (
        <p>
            Find the user in the Azure portal, then click "Reset Password", or
            use PowerZure's Set-AzureUserPassword cmdlet. If password write-back
            is enabled, this password will also be set for a synced on-prem
            user.
        </p>
    );
};

export default Abuse;
